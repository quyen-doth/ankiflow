# セキュリティポリシー

AnkiFlow のセキュリティに関する脆弱性の報告方法と、対応の方針を記載します。

## 対象バージョン

本プロジェクトは個人開発であり、過去バージョンへの修正提供（バックポート）は行いません。セキュリティ修正は常に `main` ブランチの最新状態に対してのみ適用します。

| バージョン | サポート状況 |
| --- | --- |
| `main` の最新コミット | 対応します |
| それ以前のバージョン | 対応しません |

## 報告先の使い分け

報告の内容によって、利用していただく窓口が異なります。

| 報告の内容 | 窓口 |
| --- | --- |
| 脆弱性・セキュリティ上の懸念 | Private vulnerability reporting（下記） |
| 不具合、機能要望、質問 | [Issues](https://github.com/quyen-doth/ankiflow/issues) |

脆弱性の内容を公開の Issue や Pull Request に記載しないでください。 修正が公開される前に攻撃手法が広まり、利用者が危険にさらされるためです。

## 脆弱性の報告方法

脆弱性を発見された場合は、GitHub の Private vulnerability reporting 機能をご利用ください。報告内容はリポジトリ管理者のみが閲覧でき、修正が完了するまで公開されません。

報告フォーム: https://github.com/quyen-doth/ankiflow/security/advisories/new

報告には、可能な範囲で以下の情報を含めてください。

1. 脆弱性の概要と想定される影響
2. 再現手順（該当するファイルや行番号がわかる場合は併記してください）
3. 動作確認に使用した環境（ブラウザ、OS、Anki のバージョンなど）

上記のフォームを利用できない場合は、公開の Issue に脆弱性の詳細を記載せず、連絡を希望する旨のみをお書きください。折り返し、非公開の連絡手段をご案内します。

## 対応の方針

本プロジェクトは個人が業務外の時間で開発しているため、対応期限を保証することはできません。ご報告いただいた内容は確認しだい返信し、修正の要否と対応時期についてお知らせします。

修正を公開する際は、`CHANGELOG.md` に該当項目を記載します。あわせて GitHub Security Advisory として公開し、報告者のお名前の記載を希望される場合はその旨をご連絡ください。なお、本プロジェクトでは報奨金制度（bug bounty）は設けていません。

## 対象範囲

### 対象に含まれるもの

- 本リポジトリに含まれるアプリケーションコード
- Firestore セキュリティルール（`firestore.rules`）
- 認証・認可の処理（セッション Cookie の検証、ミドルウェア、API ルートの認可）
- GitHub Actions のワークフロー定義

### 対象に含まれないもの

以下は本プロジェクトの管理範囲外のため、各提供元へご報告ください。

- Firebase、Anthropic、Google Cloud、Unsplash、LINE の各サービス自体の脆弱性
- Anki Desktop および AnkiConnect アドオン本体の脆弱性
- 利用者自身の環境設定に起因する問題（AnkiConnect の `webCorsOriginList` を過度に広く設定した場合など）

### 脆弱性として扱わないもの

以下は設計上の既知の制約であり、脆弱性としては扱いません。

- ブラウザの Local Network Access ポリシーにより、デプロイ版から `localhost:8765` への接続が遮断されること。これはブラウザ側のセキュリティ仕様であり、アプリケーション側で回避すべきものではありません。
- ミドルウェアがセッション Cookie の存在確認のみを行うこと。Edge ランタイムでは Firebase Admin SDK を実行できないため、署名の検証は各 API ルートで行う多層防御の構成としています。

## 秘密情報の取り扱い

API キーおよびサービスアカウント情報は、すべて環境変数として管理し、リポジトリには含めていません。設定すべき変数の一覧は `.env.example` を参照してください。

万一、リポジトリの履歴に秘密情報が含まれていることを発見された場合も、公開の Issue ではなく上記の Private vulnerability reporting からご連絡ください。
